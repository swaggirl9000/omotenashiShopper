import { GPTModel, GPTTemperature, ImageModel, ImageResolution, OrcishOpenAIServiceOptions, Voice, VoiceModel } from ".";
export declare class OrcishOpenAIService {
    private headers;
    constructor(options: OrcishOpenAIServiceOptions);
    private getDefaultOptions;
    getChatGPTCompletion(prompt: string, options?: {
        gptModel?: GPTModel;
        gptTemperature?: GPTTemperature;
        gptMaxTokens?: number;
    }): Promise<string>;
    getDalle3Image(prompt: string, options?: {
        imageModel?: ImageModel;
        imageResolution?: ImageResolution;
    }): Promise<string>;
    textToSpeech(prompt: string, options?: {
        voice?: Voice;
        voiceModel?: VoiceModel;
    }): Promise<Blob>;
}
